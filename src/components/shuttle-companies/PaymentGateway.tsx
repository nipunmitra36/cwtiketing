import PaymentGateway from "@/components/shared/PaymentGateway";

export default function ShuttlePaymentGateway() {
  return (
    <PaymentGateway description="Cash, cards, and contactless — accept every payment your riders already use." />
  );
}